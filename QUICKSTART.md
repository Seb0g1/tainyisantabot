# 🚀 Быстрый старт

## За 5 минут до запуска бота

### 1. Установи Python 3.11+
Проверь версию:
```bash
python --version
```

### 2. Установи зависимости
```bash
pip install -r requirements.txt
```

### 3. Создай бота у @BotFather
1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/newbot`
3. Придумай имя и username для бота
4. Скопируй токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 4. Создай файл .env
Создай файл `.env` в корне проекта и вставь:
```
BOT_TOKEN=твой_токен_здесь
```

### 5. Запусти бота
```bash
python main.py
```

Готово! 🎉 Бот запущен и готов к работе!

## Первые шаги

1. Найди своего бота в Telegram
2. Отправь `/start` в личных сообщениях
3. Создай игру в группе командой `/newgame` (нужны права админа)
4. Пригласи друзей вступить командой `/join`
5. Когда все соберутся — запусти жеребьёвку `/start_santa`

## Проблемы?

- **Бот не отвечает?** Проверь, что токен правильный в `.env`
- **Ошибка импорта?** Установи зависимости: `pip install -r requirements.txt`
- **База данных не создаётся?** Проверь права на запись в папке

## Поддержка

Если нашел баг — это фича. Если фича не работает — это баг.

Создано с любовью и 18 литрами энергетиков @Seb0g 🎅


